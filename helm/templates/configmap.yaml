apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tiny-devops-service.fullname" . }}-config
  labels:
    {{- include "tiny-devops-service.labels" . | nindent 4 }}
data:
  APP_VERSION: {{ .Values.config.appVersion | quote }}
  LOG_LEVEL: {{ .Values.config.logLevel | quote }}
